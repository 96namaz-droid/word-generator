# 🌐 Веб-версия генератора отчётов

## 📱 Работает на всех устройствах!

- ✅ **Компьютер** (Windows, Mac, Linux)
- ✅ **Телефон** (iPhone, Android)
- ✅ **Планшет** (iPad, Android)
- ✅ **Адаптивный дизайн** - интерфейс подстраивается под размер экрана

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd generator
pip install -r requirements.txt
```

### 2. Запуск веб-сервера

```bash
python start_web.py
```

Браузер откроется автоматически на `http://localhost:8000`

---

## 📱 Доступ с телефона

### Вариант 1: Локальная сеть (Wi-Fi)

1. **Запустите сервер на компьютере:**
   ```bash
   python start_web.py
   ```

2. **Узнайте IP-адрес компьютера:**
   - Windows: `ipconfig` → ищите IPv4 адрес (например, 192.168.1.100)
   - Mac/Linux: `ifconfig` → ищите inet адрес

3. **На телефоне откройте браузер и введите:**
   ```
   http://192.168.1.100:8000
   ```
   (замените на ваш IP-адрес)

### Вариант 2: Через интернет

Потребуется настройка проброса портов на роутере или использование ngrok.

---

## 🎨 Особенности интерфейса

### Для компьютера:
- Удобная форма с секциями
- Несколько колонок для эффективного ввода
- Кнопки управления всегда под рукой

### Для телефона:
- Одна колонка для удобства
- Крупные кнопки для touch-управления
- Минимальная высота 44px для всех кнопок (стандарт Apple)
- Прокрутка по секциям

### Для планшета:
- Гибридная компоновка - 2 колонки где возможно
- Оптимальное использование пространства

---

## 🔥 Возможности

### Автоматизация:
- ✅ Автоматическое получение погоды (Екатеринбург)
- ✅ Автозаполнение из базы договоров
- ✅ Автоматическое обновление базы договоров

### Управление лестницами:
- ➕ Добавление неограниченного количества лестниц
- ✏️ Редактирование характеристик для каждой
- 👁️ Визуальный осмотр для каждой лестницы отдельно
- 📋 Соответствие ГОСТ для каждой лестницы

### Валидация:
- ⚠️ Проверка обязательных полей
- ❌ Подсветка ошибок
- 📝 Подробные сообщения об ошибках

### Генерация:
- 📄 Создание Word документа
- 📥 Прямое скачивание файла
- 💾 Автоматическое сохранение в папку reports

---

## 🛠️ API Endpoints

### Основные:
- `GET /` - Главная страница
- `GET /api/weather` - Получить погоду
- `GET /api/customers` - Список заказчиков
- `GET /api/contract/{customer}` - Данные договора
- `POST /api/validate` - Валидация данных
- `POST /api/generate` - Генерация отчёта
- `GET /api/download/{filename}` - Скачать отчёт
- `POST /api/contracts/update` - Обновить базу договоров

---

## 🔧 Настройка

### Порт сервера
По умолчанию используется порт `8000`. Для изменения отредактируйте `start_web.py`:

```python
uvicorn.run("web_app:app", host="0.0.0.0", port=8888)  # Ваш порт
```

### Внешний доступ
Сервер запускается на `0.0.0.0` - доступен из локальной сети.
Для доступа только с этого компьютера измените на `127.0.0.1`.

---

## 📊 Структура проекта

```
generator/
├── web_app.py              # FastAPI приложение
├── start_web.py            # Скрипт запуска
├── web_templates/          # HTML шаблоны
│   └── index.html          # Главная страница
├── web_static/             # Статические файлы
│   ├── style.css           # Адаптивные стили
│   └── app.js              # JavaScript логика
└── README_WEB.md           # Эта инструкция
```

---

## ⚡ Производительность

- Быстрая загрузка - минимум зависимостей
- Оптимизированный CSS - без тяжелых фреймворков
- Vanilla JavaScript - без jQuery
- Асинхронные запросы - без перезагрузки страницы

---

## 🐛 Отладка

### Проверка работы сервера:
```bash
curl http://localhost:8000/api/health
```

Должно вернуть:
```json
{"status": "ok", "version": "1.0.0"}
```

### Логи:
Все действия записываются в `app.log`

---

## 💡 Советы

1. **Держите сервер запущенным** - можно минимизировать окно
2. **Добавьте в закладки** - быстрый доступ с любого устройства
3. **Используйте Wi-Fi** - быстрее чем мобильный интернет
4. **Проверьте файрволл** - должен пропускать порт 8000

---

## 🆚 Сравнение с Tkinter версией

| Функция | Tkinter | Web |
|---------|---------|-----|
| Работает на ПК | ✅ | ✅ |
| Работает на телефоне | ❌ | ✅ |
| Работает на планшете | ❌ | ✅ |
| Доступ из сети | ❌ | ✅ |
| Адаптивный дизайн | ❌ | ✅ |
| Установка на устройство | ✅ | ❌ |
| Работает без интернета | ✅ | ✅* |

*Веб-версия работает в локальной сети без интернета

---

## 📞 Поддержка

При возникновении проблем проверьте:
1. Установлены ли все зависимости (`pip install -r requirements.txt`)
2. Свободен ли порт 8000
3. Не блокирует ли файрволл
4. Находятся ли устройства в одной сети (для доступа с телефона)

---

**Готово! Теперь вы можете работать с любого устройства! 🎉**

